import { Routes } from '@angular/router';
import { ScheduleGeneratorPageComponent } from './schedule/page/schedule-generator.page.component';

export const APP_ROUTES: Routes = [{ path: '', component: ScheduleGeneratorPageComponent }];
